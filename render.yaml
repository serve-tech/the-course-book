# Render Blueprint for coursebook.golf. This file is the source of truth for
# Render infrastructure; secret values are entered in the dashboard (sync: false).
# Schema: https://render.com/schema/render.yaml.json
databases:
  - name: coursebook-db
    plan: basic-256mb
    region: oregon
    postgresMajorVersion: "17"

# The API web service and the static web app are added with the API split
# (.planning/decisions/2026-09-25-split-into-json-api-service-for-web-ios-and-android.md).
